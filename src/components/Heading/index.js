import React from "react";
import './index.css';

const Heading = ({ title }) => {
  return (
    <div className="heading-container">
      <h2>{title}</h2>
    </div>
  );
};
export default Heading;
